{
  "name": "prac-js",
  "private": true,
  "version": "1.0.0",
  "description": "this is a repository for freely experimenting with javascript and typescript.",
  "author": "koichi hayakawa",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "next:dev": "yarn workspace next dev",
    "next:format:fix": "yarn workspace next format:fix",
    "next:lint:fix": "yarn workspace next lint:fix",
    "fastify:start": "yarn workspace fastify start",
    "fastify:format": "yarn workspace fastify format"
  }
}
